<Window x:Class="VncTrayApp.RequestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Запрос управления"
        Height="240" Width="440"
        WindowStyle="None" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" Topmost="True"
        AllowsTransparency="True" Background="Transparent">

    <Border Background="#FFFFFF"
            BorderBrush="#E0E0E0"
            BorderThickness="1"
            CornerRadius="16">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Direction="270" 
                             RenderingBias="Quality" ShadowDepth="3"
                             Color="#000000" Opacity="0.15"/>
        </Border.Effect>

        <Grid Margin="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" 
                       Margin="24,18,24,12">
                <Grid Width="24" Height="24" Margin="0,0,12,0">
                    <Ellipse Fill="#0078D7" Opacity="0.8"/>
                    <TextBlock Text="⚙" FontSize="12" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <TextBlock Text="Hardware Agent" FontSize="14" FontWeight="SemiBold"
                           Foreground="#2C2C2C" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock Grid.Row="1"
                       x:Name="textBlock"
           Text="Администратор AdminName запрашивает доступ к вашему рабочему столу."
           TextWrapping="Wrap"
           TextAlignment="Left"
           FontSize="15"
           FontWeight="Medium"
           Foreground="#333333"
           Margin="32,4,32,3"
           MaxWidth="300"
           LineStackingStrategy="BlockLineHeight"/>

            <Border Grid.Row="2" Background="#F8F8F8" CornerRadius="0,0,16,16"
                    Padding="24">
                <UniformGrid Rows="1" Columns="2" HorizontalAlignment="Right" Margin="24">
                    <Button Content="Отклонить" Width="100" Height="36"
                            Click="Deny_Click" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#F0F0F0"/>
                                <Setter Property="BorderBrush" Value="#D0D0D0"/>
                                <Setter Property="Foreground" Value="#555555"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="FontWeight" Value="Medium"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E5E5E5"/>
                                        <Setter Property="BorderBrush" Value="#C0C0C0"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#D5D5D5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Разрешить" Width="120" Height="36"
                            Click="Allow_Click" Cursor="Hand" FontWeight="SemiBold">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#0078D7"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="0,2"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#005A9E"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#004578"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </UniformGrid>
            </Border>
        </Grid>
    </Border>
</Window>
